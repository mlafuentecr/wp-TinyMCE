function my_mce_buttons( $buttons ) {	
	/**
	 * Add in a core button that's disabled by default
	 */
	$buttons[] = 'superscript';
	$buttons[] = 'mceInsertLink';
 // Debug:
// print_r( $buttons );

	return $buttons;
}
add_filter( 'mce_buttons_2', 'my_mce_buttons' );
